<?xml version="1.0" encoding="utf-8"?>
<!--
    Cloud Backup / Device Transfer rules for ContextOS.
    We exclude all sensitive data (tokens, cached sensor data) from backups.
    Room databases and SharedPreferences containing auth tokens must never leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
